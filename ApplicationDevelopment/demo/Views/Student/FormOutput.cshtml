@model demo.Models.Student

@{
    var age = Model.Age;
    var year = DateTime.Now.Year;
    var yob = year - age;
}

Name: @Model.Name <br />
Age: @Model.Age <br />
Yob: @yob <br />
Gender: @Model.Gender <br />
Graduated: @Model.IsGraduated